import type { FunctionComponent } from "react";
import SettingsScreen from "../screens/Settings";

const SettingsRoute: FunctionComponent = () => {
	return <SettingsScreen />;
};

export default SettingsRoute;
